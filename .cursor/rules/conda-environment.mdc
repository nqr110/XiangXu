---
description: 项目使用项目内 .venv 虚拟环境开发
alwaysApply: true
---

# 项目虚拟环境

本项目使用**项目根目录下的 `.venv`** 虚拟环境，不依赖 conda。

## 一键运行（推荐）

- **Windows**：双击或在终端执行 `run.bat`，会自动创建 `.venv`、用国内源安装依赖（若已存在且已安装则跳过），并启动主程序。
- 需已安装 Python 3.8+（未安装时 run.bat 会提示）。

## 约束

- 运行 Python、安装依赖、执行测试时，应使用 `.venv` 中的解释器：`.venv\Scripts\python.exe`（Windows）或先 `source .venv/bin/activate` 再 `python`。
- 新增依赖写入 `requirements.txt`，并通过 `pip install -r requirements.txt` 安装。`run.bat` 使用国内源：首选中科大，失败则依次尝试清华、阿里云。

## 示例

```bash
# Windows（项目根目录下）
.venv\Scripts\python.exe main.py
.venv\Scripts\pip.exe install -r requirements.txt -i https://mirrors.ustc.edu.cn/pypi/simple --trusted-host mirrors.ustc.edu.cn
```
