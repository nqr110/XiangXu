Debug_Mode=False
# DASHSCOPE_API_KEY=sk-xxx   # 可选，也可在设置页输入

# 控制台主窗口：启动时的宽高（若在设置页保存过则优先用 config.json）
CONSOLE_WIDTH=1104
CONSOLE_HEIGHT=620
# 控制台主窗口：最小宽高（不可拖得更小；启动时若上面宽高小于此处则以此处为准）
CONSOLE_MIN_WIDTH=920
CONSOLE_MIN_HEIGHT=620
